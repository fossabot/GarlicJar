import { StyleSheet } from "react-native";

import { colours } from "../coreStyles";

export default StyleSheet.create({
    formInput: {
        marginRight: 16,
        fontSize: 16,
        color: colours.dark
    }
});
